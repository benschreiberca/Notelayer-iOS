Native SwiftUI iOS app for Notelayer

## Build & Run (Xcode)
- Open `Notelayer.xcodeproj` in Xcode.
- Select an iOS Simulator (or a connected iPhone).
- Press **Run** (⌘R).

## Versioning
This repository starts at **v3.0.0**. The Notelayer web app is versioned separately and is not tracked in this repo.

